---
name: codex-auth-switching-field-guide
description: 当需要按当前电脑环境为 Codex 设计、审查或修复官方 OAuth 账号与 API 兼容接口切换方案，并保留实时配置、凭据、扩展能力和本地对话兼容性时使用。
---

# Codex 账号与接口切换经验指南

为当前电脑构建有针对性的方案，不照搬所谓“通用切换器”。

> 英文次选版本：[SKILL.en.md](SKILL.en.md)

## 适用场景

当用户需要组合使用官方 OAuth 账号、API 兼容接口、不同供应商模型、实时配置保留、跨模式本地历史或代理／传输诊断时使用本指南。

本指南不用于迁移云端 ChatGPT 会话、绕过账号规则或跨用户分享凭据。

## 说明书定位与实现自由度

本仓库是给智能体参考的产品说明书和经验包，不是可直接部署的成熟程序，也不是固定技术方案。先理解用户真正要解决的问题，再结合可读取的本机系统、Codex 版本和现有工具选择实现；可以使用脚本、桌面小应用、系统原生档案或其他合适方式，也可以按用户要求增加、删除或替换功能。

仓库中的 Windows 路径、字段和流程只代表已验证案例。其他系统使用等价机制；任何版本相关结论都应重新探测。不要询问能够从本机安全读取的事实，只询问无法推断且会改变成品的用户选择。

## 一句话部署的权限边界

用户从首页复制“一句话部署”时，只代表允许只读盘点并为本机构建方案；这不等于允许暴露凭据、静默安装第三方程序、切换当前账号或修改对话历史。真正需要执行这些敏感操作时，必须在写入前单独说明并获得确认。

## 工作流程

1. 先做只读盘点：确认系统与 Codex 版本、进程、用户级配置、原生配置档案、认证存储、当前路线、模型、历史存储、代理行为，以及 CC Switch 等外部配置管理器。
2. 只报告认证类型和一致性，不把密钥、令牌或完整认证文件输出到对话。
3. 根据盘点结果集中询问仍需用户决定的内容：目标账号和中转数量、是否兼容旧对话、希望使用的界面、可接受的停机方式，以及哪些模型或代理策略需要绑定档案。不要重复询问已能读取的环境事实。
4. 与用户确认本次最小范围；后续步骤是可组合模块，不要求每台电脑实现所有功能。
5. 确立唯一配置写入者；如果多个工具都会投射或重写同一个实时 `config.toml`，先让用户选择其中一个。
6. 第一次写入前，提醒用户自行保存一份已经在全新 Codex 进程中完成真实请求的完整 `config.toml`；不要静默积累无限备份。
7. 明确切换器负责的字段，只修改路线、模型和经过当前版本验证的代理／传输字段；插件、MCP、技能、钩子、权限、项目和未知新字段继续以实时配置为准。
8. 把路线、认证和历史兼容拆成三个状态域，不能只换其中一个就宣布成功。
9. 根据实测起点设计首次使用：明确处理“从未官方登录”和“从未配置 API／中转”两种情况，不能制造不存在的档案。
10. 接口地址和模型可以询问；密钥必须通过本地遮罩输入或操作系统支持的入口收集，不能要求用户粘贴进聊天。
11. 用户需要多个中转站时，每个档案保存地址、受保护凭据引用、模型和代理策略，不保存整份配置，从而可以不再让 CC Switch 管理 Codex。
12. 先在已安装版本和目标接口上证明共同 provider 身份。只有验证通过，官方和 API 兼容档案才共同使用 `model_provider = "openai"`；不得用 `[model_providers.openai]` 重定义内置名称。
13. 把首次初始化实现为可重复恢复的检查点流程；每次必须重启 Codex 前先保存非敏感进度，再由全新进程验证实际生效状态。
14. 若用户要求旧对话跨模式续聊，首次跨模式前盘点当前及归档 JSONL 与全部相关 SQLite 的 provider 值，排除云端 ChatGPT Work／Chat 记录。
15. 只有在用户确认且所有写入者退出后，才能按结构统一已确认的本地 provider 字段；JSONL 使用内容备份和变更清单，SQLite 使用数据库感知备份，不能假设只改第一行。
16. 完整历史准备独立于普通切换；provider 统一和响应项目编号兼容使用两套规则，普通切换只检查便宜的历史指纹。
17. 为多文件写入设计事务日志和回滚；具体机制可随平台改变，闲置凭据应使用系统提供的保护能力。
18. 为配置清空、语法损坏或通用模板化提供离线恢复路线；Codex 无法对话时，应允许本地脚本或另一个编程智能体在不读取凭据值的前提下修复。
19. 在相关重启后验证用户选择范围内的真实路线、认证类型、模型、代理、传输、新会话 provider、代表性旧会话续聊、回滚和无关配置保留。
20. Codex 升级后重新盘点并复测路线、传输和历史契约，不能直接沿用旧结论。

## 不可违反的安全规则

- 不得显示、记录、提交或让模型读取凭据值。
- 不得仅凭重启前文件正确就把新档案标为可用。
- 不得为了切换认证而替换整份实时配置。
- 不得让两个档案管理器轮流拥有同一个实时 `config.toml`。
- 不得把推断或重建的值伪称为“恢复出的原值”。
- 不得在 JSONL 仍被写入时整体重写，也不得直接复制正在使用的 WAL 数据库作为完整备份。
- 不得全局替换 provider，也不得假设旧名称与 `openai` 等长。
- 不得把云端 Work／Chat 记录当成本地 rollout 修改。
- 不得按字符串前缀盲目改响应项目编号；必须按语义类型分类。
- 缺少目标接口要求的受保护推理内容时，不得伪造 reasoning 项。
- 必须保留工具调用与结果的配对关系。
- 预检查或兼容门禁失败时，在第一次写入前停止。
- 回滚只能撤销清单中记录的内容，不能覆盖用户后来产生的新活动。

## 按需阅读

- 从[环境盘点](references/discovery.md)开始。
- 实施前阅读[总体设计](references/architecture.md)。
- 缺少任一认证模式或必须重启验证时，阅读[首次使用与重启检查点](references/first-use-bootstrap.md)。
- 存在 CC Switch、配置字段消失或 Codex 已无法对话时，阅读[配置恢复与外部写入者](references/config-recovery.md)。
- 用户需要多个中转站且不再使用 CC Switch 时，阅读[多中转档案](references/multi-relay-profiles.md)。
- 旧对话必须跨模式续聊时，阅读[历史兼容](references/history-compatibility.md)。
- 出现重连、超时、代理或接口差异时，阅读[网络诊断](references/network-diagnostics.md)。
- 任何写入前阅读[安全与回滚](references/safety-and-rollback.md)。
- 最后使用[验证清单](references/validation.md)。

## 交付物

最终方案应覆盖用户实际选择的范围，并至少说明：脱敏状态盘点、字段所有权、首次使用、凭据边界、失败恢复、验证结果和已知限制。多中转、历史准备、图形界面等未被用户选择的模块可以不实现；一旦实现，就按对应参考文档闭环。
